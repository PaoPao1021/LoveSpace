<view class="particle-container">
  <text
    wx:for="{{particles}}" wx:key="id"
    class="particle {{item.isHeart ? 'particle-heart' : 'particle-sparkle'}}"
    style="{{item.style}}"
  >{{item.isHeart ? '💕' : ''}}</text>
</view>
